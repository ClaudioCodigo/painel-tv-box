# Devices — arquivos locais por TV Box
#
# Cada TV Box é um YAML aqui (ex.: `tv-box-pier.yml`), criado pelo wizard ou
# pela API. **Estes arquivos NÃO são versionados** (`.gitignore`): contêm IPs
# e configuração específicos de cada máquina — não devem ir no `git push`.
#
# Para adicionar: use o painel → aba "Dispositivos" → "Novo TV Box",
# ou o wizard (`#/wizard`).
