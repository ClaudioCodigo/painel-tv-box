---
context: research
phase: new-project-initialization
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-08-06T14:48:17.032Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Runtime pi sem subagentes/ask** — este runtime não tem `Agent`/`spawn_agent` nem `AskUserQuestion`. A pesquisa e o mapeamento foram feitos INLINE (fallback `sequential_mapping` do workflow). **No OpenCode você TEM subagente e ask** — use `gsd-roadmapper` (e, se re-pesquisar, `gsd-project-researcher`) e apresente as perguntas restantes ao usuário em vez de decidir sozinho.
- [ ] CONSTRAINT: **Regra absoluta do projeto (IDEA.md)** — NÃO inventar requisitos; apresentar alternativas com prós/contras antes de decisões arquiteturais. O usuário já confirmou o escopo desta fase; não adicionar features novas (ex.: captura Office) sem confirmar.
- [ ] CONSTRAINT: **Requisito Office esclarecido** — o painel NÃO faz captura. O objetivo do cliente é transmitir a suíte Office; a migração Windows-only é a resposta. Captura de tela de apps Office = **anti-feature** em serviço Windows (sem desktop, sessão 0). Não implementar.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Decidir sem perguntar | Runtime pi sem ask → risco de defaults silenciosos (#3018) | blocking | No OpenCode usar ask/request_user_input para TODAS as perguntas de workflow; nunca defaultar silenciosamente |
| Pesquisar com subagente em runtime que não tem | gsd-project-researcher falharia aqui | advisory | No OpenCode os agentes existem; se faltarem, rodar pesquisa inline (como feito) e rotular |

_Remove rows that do not apply. The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Inicialização GSD do **Painel TV Box** (brownfield, projeto existente com 26+ commits e 111 testes) está no meio do `$gsd-new-project`:

- ✅ **Step 1-2**: codebase mapeado — `.planning/codebase/` (7 docs, commit `e67c8b7`)
- ✅ **Step 5**: config gravada — `.planning/config.json` (yolo/coarse/parallel/adaptive, commit `61958f3`)
- ✅ **Step 4**: PROJECT.md — `.planning/PROJECT.md` (commit `82f3dfd`)
- ✅ **Step 6**: pesquisa de projeto — `.planning/research/` (5 docs, commit `ac7a2a4`, feita INLINE com Tavily)
- ⏳ **Step 7**: REQUIREMENTS.md — NÃO feito
- ⏳ **Step 7.5**: PROJECT_MODE=mvp — já decidido (usuário escolheu 8:1, MVP vertical)
- ⏳ **Step 8**: ROADMAP.md + STATE.md + traceability — NÃO feito (spawn `gsd-roadmapper`)
- ⏳ AGENTS.md + commit final + sumário — NÃO feito
</current_state>

<completed_work>

- Map de codebase completo: STACK, INTEGRATIONS, ARCHITECTURE, STRUCTURE, CONVENTIONS, TESTING, CONCERNS (`.planning/codebase/`)
- Config de workflow gravada e commitada: mode=yolo, granularity=coarse, parallelization=true, commit_docs=true, model_profile=adaptive, workflow.research/plan_check/verifier=true, plan_review.source_grounding=true, ship.pr_body_sections=[]
- PROJECT.md brownfield: Validated (capacidades existentes do codebase), Active (WIN-01..WIN-06 da migração), Out of Scope (captura Office, Linux/macOS, features novas), Key Decisions (NSSM, C:\PanelTVBox com .git, deploy/legacy/, simplificação)
- Pesquisa de projeto (inline): STACK (NSSM 2.24, PowerShell, winget-fallback, MediaMTX via API GitHub), FEATURES (table stakes/anti-features), ARCHITECTURE (2 serviços NSSM + firewall LAN + publicadores externos), PITFALLS (10 armadilhas: espaços no NSSM, PATH do serviço, .git, firewall, test_platform_info_linux), SUMMARY (3 fases sugeridas)
- Decisões do usuário coletadas (todas confirmadas, ver HANDOFF.json)
</completed_work>

<remaining_work>

- **Step 7 — REQUIREMENTS.md**: usar escopo já confirmado (WIN-01..WIN-06) com REQ-IDs; categorias sugeridas: INFRA (instalador), SVC (serviços NSSM), SEC (firewall), CLEAN (limpeza Linux), DOC (docs). Apresentar ao usuário para scoping v1/v2/out-of-scope (multi-select via ask no OpenCode).
- **Step 7.5**: PROJECT_MODE=mvp (já decidido — não perguntar de novo)
- **Step 8 — ROADMAP.md**: spawn `gsd-roadmapper` com contexto (PROJECT.md, REQUIREMENTS.md, research/SUMMARY.md, config.json); 3 fases sugeridas na pesquisa (Instalador Windows → Refatoração Windows-only → Docs). Emitir `**Mode:** mvp` sob cada `**Goal:**`. Validar 100% de cobertura; atualizar traceability em REQUIREMENTS.md; criar STATE.md.
- **Aprovação do roadmap** (ask ao usuário: approve/adjust/review) antes do commit final.
- **Gerar AGENTS.md** (`gsd_run query generate-claude-md --output AGENTS.md`) e commit final `docs: create roadmap (N phases)`.
- Sumário de conclusão + `$gsd-discuss-phase 1`.
</remaining_work>

<decisions_made>

- Windows-only; serviços via NSSM com auto-restart (Opção A aprovada); binários baixados pelo install.ps1; `C:\PanelTVBox` preservando `.git`; data dir `%LOCALAPPDATA%\PanelTVBox`; `instalar.bat` launcher
- Deploy Linux arquivado em `deploy/legacy/` (não remover); simplificar `_platform_info`/`get_data_dir` p/ Windows-only (ajustar `test_platform_info_linux`)
- Requisito Office = transmitir suíte Office; painel NÃO captura; captura = anti-feature em serviço Windows
- Config: YOLO/coarse/parallel/adaptive; research+plan_check+verifier+drift guard ON; PR body sem seções; MVP vertical
- Usar `AppEnvironmentExtra` (NUNCA `AppEnvironment`) no NSSM — preserva ambiente do sistema
</decisions_made>

<blockers>
- Nenhum blocker técnico. Só a limitação de runtime (subagentes/ask indisponíveis aqui) — resolvida migrando para o OpenCode.
</blockers>

## Required Reading (in order)
<!-- List documents the resuming agent must read before acting -->
1. `docs/HANDOFF.md` — handoff da sessão anterior (tarefas 1-3, decisões, assets validados, comandos NSSM/firewall de referência)
2. `.planning/PROJECT.md` — contexto do projeto + requisitos Active/Out of Scope
3. `.planning/research/SUMMARY.md` — síntese da pesquisa + 3 fases sugeridas + armadilhas
4. `.planning/codebase/CONCERNS.md` — dívida técnica e riscos do deploy Windows
5. `.planning/config.json` — preferências de workflow
6. `.planning/METHODOLOGY.md` (se existir) — analítico do projeto

## Critical Anti-Patterns (do NOT repeat these)
<!-- Mistakes discovered this session that must be structurally avoided -->
- [RUNTIME]: este runtime não tem subagente/ask → pesquisa inline. No OpenCode, usar subagentes e ask; não decidir defaults silenciosamente (#3018).
- [OFFICE]: não implementar captura de apps Office no painel (anti-feature); manter o requisito como contexto da migração.

## Infrastructure State
<!-- Running services, external state, environment specifics -->
- Nenhum serviço rodando (projeto em estado de código, não deploy).
- `.venv/` existe (Python 3.11/3.13 usados nos testes).
- Git: `main` com 4 commits GSD novos (e67c8b7, 61958f3, 82f3dfd, ac7a2a4) sobre f26b4ad.
- **Não commitado (pré-existente)**: `app/utils/system.py` modificado (não mexido nesta sessão — revisar antes de commitar); `docs/HANDOFF.md` (novo); PDF não relacionado; artefatos de sessão do harness (`pi-session-*.jsonl/html`) — não versionar.
- Config local `config/system.yml` real existe (gitignored) com `wizard_completed` — ambiente de dev do usuário.

## Pre-Execution Critique Required
<!-- Fill in ONLY if pausing between design and execution (e.g. spike design done, not yet run) -->
- N/A (não há design a executar ainda — a inicialização continua).

<context>
Fluxo `$gsd-new-project` em andamento, modo texto (sem AskUserQuestion). O usuário respondeu TODAS as perguntas do workflow e confirmou o escopo integralmente: migração Windows-only com install.ps1/NSSM/firewall LAN, limpeza do caminho Linux, simplificação Windows-only, docs. A pesquisa de projeto foi feita inline e validada com Tavily (NSSM 2.24 estável, MediaMTX windows_amd64, ffmpeg gyan.dev Windows 10+). O usuário vai continuar no OpenCode porque lá tem subagente e ask — o roadmap (Step 8) deve usar `gsd-roadmapper` como subagente.
</context>

<next_action>
Start with: No OpenCode, rodar `$gsd-resume-work` (ou retomar `$gsd-new-project` manualmente no Step 7): criar `.planning/REQUIREMENTS.md` com os requisitos WIN-01..WIN-06 (REQ-IDs), apresentar ao usuário via ask para scoping, depois Step 8: spawn `gsd-roadmapper` (PROJECT_MODE=mvp) para ROADMAP.md/STATE.md/traceability, aprovar com o usuário, gerar AGENTS.md e commitar.
</next_action>
