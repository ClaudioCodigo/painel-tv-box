# Grupos — arquivos locais
#
# Cada grupo é um YAML aqui (ex.: `administracao.yml`), criado pelo painel.
# **Não versionado** (`.gitignore`) — configuração local, não vai no `git push`.
# Gerencie pelo painel: aba "Grupos" → "Novo Grupo".
